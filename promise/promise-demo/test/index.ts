import * as chai from 'chai';
import 'mocha';

const assert: any = chai.assert;

/* describe('Promise', () => {
    it('是一个类', () => {

    })
}) */

describe('Chai 的使用', () => {
    it('可以测试相等', () => {
        assert(1 === 1);
    })
})